## Example .env for backend
# Copy this file to `backend/.env` and fill in your values.
# Do NOT commit real credentials to source control.

# Example using SQLite (local dev):
# DATABASE_URL=sqlite:///./db.sqlite3

# Example using Postgres (Supabase): percent-encode special characters in the password.
# DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<dbname>?sslmode=require

# If your password contains special characters, encode them. Example:
# password: pa$$word -> pa%24%24word
